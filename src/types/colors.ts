export interface IColorType {
  first: string
  secondary: string
}

export interface IColors {
  red: IColorType
  orange: IColorType
  yellow: IColorType
  green: IColorType
  blue: IColorType
  pink: IColorType
  purple: IColorType
}